# Security
SECRET_KEY=your_super_secret_key_change_in_production_12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email — use SMTP (no Azure App Registration needed)
# Set EMAIL_METHOD=graph only when you have Azure App with Mail.Send
EMAIL_METHOD=smtp # or graph if you have Azure App with Mail.Send
MAIL_USERNAME=siva.kumar@dotkonnekt.com # or your production email
MAIL_PASSWORD=ASDF@1234 # or your production email password
MAIL_FROM=siva.kumar@dotkonnekt.com # or your production email
MAIL_PORT=587 # or your production email port
MAIL_SERVER=smtp.office365.com # or your production email server

# Optional: Microsoft Graph (only if you have Azure App Registration with Mail.Send)
# Uncomment and set these, then set EMAIL_METHOD=graph to send "from manager"
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=
# Or use a manual token (expires ~1 hour): GRAPH_API_TOKEN=

# Admin (used by POST /admin/bootstrap)
ADMIN_EMAIL=admin@dotkonnekt.com # or your production admin email
ADMIN_PASSWORD=Admin@123 # or your production admin password
ADMIN_EMPLOYEE_ID=ADMIN001 # or your production admin employee ID
ADMIN_FULL_NAME=Super Admin # or your production admin full name

# Database
MYSQL_HOST=localhost
MYSQL_PORT=3306 # or your production database port
MYSQL_USER=root # or your production database username
MYSQL_PASSWORD=adminadmin # or your production database password
MYSQL_DATABASE=leave_management_db # or your production database name

# Frontend
FRONTEND_URL=http://localhost:3000 # or your production URL

# Allow bootstrap no auth (set false after first admin exists)
ALLOW_BOOTSTRAP_NO_AUTH=true # or false if you have already created an admin user
